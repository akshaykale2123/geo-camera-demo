# Geo + Camera Permission Demo

This is a security / education demo.

It:
- Asks for location permission (geolocation)
- Asks for front camera permission
- Captures one frame from the camera
- Sends location + image + userAgent to `/api/capture`
- Backend prints it to server logs (no database)

**Use only with explicit consent.**
